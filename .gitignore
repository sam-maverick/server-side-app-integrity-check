# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

upstream_push.sh

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/

scratchpaper/

# Native
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Android artifact files
*.apk
*.aab

# Local git used by EAS compiler. Rename .git to .git2 before initial commit, then rename .git2 to .git and commit again
.git2

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local

# typescript
*.tsbuildinfo

# Ignore Gradle project-specific cache directory
.gradle

# Ignore Gradle build output directory
build

# npx expo-doctor  
# This project has native project folders but is also configured to use Prebuild. EAS Build will not sync your native configuration if the ios or android folders are present. Add these folders to your .gitignore file if you intend to use prebuild (aka "managed" workflow).
android/
ios/
